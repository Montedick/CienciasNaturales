<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.montelongo.cienciasnaturales">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icono_inicio"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icono_inicio"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Inicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu" />
        <activity android:name=".listaHombreMujer" />
        <activity android:name=".salud" />
        <activity android:name=".ecosistema" />
        <activity android:name=".efectos" />
        <activity android:name=".infPersona" />
        <activity android:name=".video_ecosistema"></activity>
    </application>

</manifest>